name: control_your_weight
description: Application for weight control
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  fl_chart: ^0.41.0
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
